# 工作区记忆（{{工作区}}）

> **热记忆**：本文件只放**提示词、规则、长期习惯**。事实与数字都在别处，按下面的导航去读，不要抄进来。
> 定位：{{一句话定位}}
> 最后更新：{{日期}}

## 要什么去哪读

| 需要什么 | 去哪读 |
| --- | --- |
| 环境事实：端点、Key、路径、模型、版本、口径坑 | [`memory/settings.md`](memory/settings.md) |
| 当前数字：体积/行数、缓存、归档、磁盘、设备、白名单 | [`STATE.md`](STATE.md)（**脚本生成、勿手改**） |
| 已定型、可照做的流程 | [`handbook/`](handbook/README.md) |
| 在做的事、试出的结论、待办 | `memory/YYYY-MM-DD/`（索引 [`memory/README.md`](memory/README.md)） |
| 要再跑的脚本与产物 | [`scratch/`](scratch/README.md) |
| 出题/作答的受控交流（多智能体测试） | [`exchange/`](exchange/README.md) |
| 不可再生的数据快照 | [`archive/`](archive/README.md) |
| 用过不再读的产物 | [`trash/`](trash/README.md) |

## 工作方式（提示词）

1. **先定位再动手**：按上表只读必要的文件；要动数据或配置前先做**只读探测**。
2. **交付前实跑，并说明验到哪一层**：逻辑层（行数/关系对得上）／文件层（sha256）／
   **设备层**（`dd iflag=direct` 绕过页缓存）／新鲜度层（数据是不是最新）。踩过：sha256 只花 0.17 秒
   就"通过"，其实读的是页缓存 —— 那不算验证。
3. **复核的取舍**：核验别人结论时，**小而短、质量高的步骤可以自行重复**（复跑基准、比对原始输出）；
   **长而繁琐、质量低的步骤不必重复**（重跑一轮耗时数分钟的模型调用），别为核验烧时间和额度。
4. **外部副本 / 对外发布前先扫敏感串**（体检脚本已含这条）。
5. **不确定就不猜**：写明卡点与验证边界；不用近似或模拟数据冒充真实结果。

## 规则

- **成功判据**：CLI 看**退出码 0 且 JSON 信封 `ok=true`**；远端 REST/MCP 看业务 `code=0`。
- **密钥卫生**：Key 只允许出现在 `memory/settings.md`（且需在 `.memory-kit.toml` 里显式豁免）
  或用户级凭据文件（`chmod 600`）；**不进脚本、日志、命令参数、外部副本**。
- **单写者**：同一份可变资源（数据库、索引、配置）同一时刻只允许一个写进程；别与会话/定时任务并发。
- **资源红线**：用户给过的额度与上限（磁盘、时间、额度）要**主动提醒**，别等撞墙。
- **非 git 仓库**；若日后 `git init`，推远端前必须先跑敏感串扫描并处理明文 Key。
- **制度本体在技能里**：本文件是**本工作区的实例**；规则怎么改、脚本怎么用，见技能 `agent-memory-kit`
  的 `SKILL.md`（技能放在哪由你的环境决定；工作区里的薄壳已经填好本机路径）。

## 长期习惯（落盘）

- **批量落盘**：一件事没做完不写记忆；一个阶段全部到位后**一次性**补/改，别边做边写（改动成块、频次低）。
- **同类合并**：同一天、同一类事情**在同一个文件上改**，不要一天摊一堆文件；跨天续写就在新文件开头写明"接 XXX"。
- **压缩两档**：单篇到触发线（默认 **150 行**）就**压到 80 行**（删过程史，细节外移到脚本/`handbook/`/`scratch/`）；
  **实在压不到 80** 才退档 —— 上限 +50（→200）、下限 +40（→120），即老实落在 **120~200 行**，
  并在末尾留一行「压缩记录：删了什么、去哪了、为何到不了 80」。体检：过触发线 ⚠️，超硬上限 ❌。
- **结论前置**：超过 ~80 行的文档，开头 12 行内给「结论 / 怎么做 / 坑」。
- **易变数字不手写**：数字交给 `STATE.md` 的生成脚本，半稳定事实写进 `memory/settings.md`。
- **定型就搬家**：彻底完善且实测通过的流程/结论 → `handbook/`（来源笔记里压缩成指针）。
- **测试走协议**：多智能体测试一律用 `exchange/` —— 出题方写 `task.md` 并建一个空的 `answer.md`；
  作答方只写 `answer.md`；结论由出题方复核后写入记忆。目的不是防谁，而是**写入范围可查、复盘有据**。
- **命名规则**：**记忆与交流按时间**（`memory/YYYY-MM-DD/主题.md`、`exchange/YYYY-MM-DD-主题/`）；
  **其余一律按种类**（`scratch/<种类>/`、`handbook/<主题>.md`、`archive/<种类>/<快照日期>/`、`trash/<种类>/`）。
  同一种类的新脚本/新批次**就地加文件、或让文件名带日期**，不再新建日期目录。
- **落盘六区**：结论/过程 → `memory/`，可照做 → `handbook/`，可复跑 → `scratch/`，出题作答 → `exchange/`，
  数据快照 → `archive/`，要扔 → `trash/`（`mv` 不 `rm`）。
- **写完笔记跑三步**：`memory_query.py --rebuild`（台账跟上磁盘）→ `--refresh`（刷新各区 README 活窗口）→
  `memory_doctor.py`（体检）；动过数据/盘/配置再跑 `state_snapshot.py`。
- **台账 ≠ 散文**：全量索引在 `memory/index/YYYY-MM.json` 与其他区的 `index.json`（**数据**，不参与 md 行数规则）；
  区 README 只放规则 + 活窗口，**不要手写索引行**（README 列全量台账是 O(条目数)，早晚撞死行数硬上限）。
- **阅读面要有预算**：`memory_query.py --stats` 看正文总量；超预算就结账
  （`--prune --before <日期> --apply` 批量 mv 进 `trash/`，`--delete --path P` 单条），**别让 agent 想着记住一切**。
