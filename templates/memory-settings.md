# 环境事实（settings）

> **半稳定事实的唯一出处**：端点、凭据位置、路径、模型名、版本、口径坑。
> 热记忆 `AGENTS.md` 不抄这里的内容；**易变数字**（体积/行数/磁盘）也不写这里 —— 归 `STATE.md` 生成脚本。
> 改这里不用新开对话；但**密钥一旦写进来，就必须同步在根目录 `.memory-kit.toml` 里显式豁免**，
> 否则体检会把它判成 ❌（这是有意的：豁免是一个需要人确认的决定）。
> 最后更新：{{日期}}

## 凭据

| 用途 | 位置 | 说明 |
| --- | --- | --- |
| <服务名> | `~/.<app>/credentials.env`（0600） | <优先级/环境变量名；**只写位置，不写值**> |
| <本机服务> | 本文件（本节） | <确有必要才放；放进来后必须在 `.memory-kit.toml` 豁免本文件> |

密钥卫生（硬规则）：

- Key **不进**脚本、命令参数、日志、报错、`STATE.md`、外部副本；
- 打印时只留掩码（前 6 + 后 4）；
- `git init` 之前必须把明文 Key 移出仓库；
- 给外部的人/机器看的东西，先跑 `memory_doctor.py --only secrets`。

## 端点

| 用途 | 地址 | 备注 |
| --- | --- | --- |
| <服务> | `<http://host:port>` | <协议前缀 /v1；UA 要求；超时> |

## 工具与版本

| 工具 | 版本 | 路径 | 备注 |
| --- | --- | --- | --- |
| <CLI> | <x.y.z> | `<绝对路径或 PATH>` | <升级方式、自更新开关> |

## 路径与目录

| 项 | 值 |
| --- | --- |
| 数据目录 | `<绝对路径>` |
| 缓存目录 | `<绝对路径>` |
| 锁文件 | `<绝对路径>`（单写者保护） |

## 口径坑（踩过的，别再踩）

- <一句话：现象 → 根因 → 正确做法>
- <例：时间戳字段是**北京时间零点**，不是 UTC；按 UTC 解释会整体偏一天。>

## 维护

- 事实**只在这里写一次**，其它文档用链接指过来。
- 发现与实测不符：先改这里，再回改引用它的文档（体检会查断链，但查不出"话说过时了"）。
