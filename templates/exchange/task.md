# 测试文档（task）：{{主题}}

> 一句话说明：{{说明}}
> 本文件由**出题方**书写与维护；作答方**不要修改本文件**。
> 建新测试：`bash scratch/memory-tooling/new_exchange.sh "YYYY-MM-DD-主题" "说明"`

```
状态: pending
出题方: lead
作答方: <模型或 teammate 名>
出题时间: YYYY-MM-DD HH:MM +0800
代价上限: <例如 5 分钟 / 1 次模型调用>
```

## 一、目标

<一句话说清要回答什么；必须是可判定对错的问题>

## 二、输入与位置

| 项 | 值 |
| --- | --- |
| 数据/文件 | <绝对或工作区相对路径；没有就写"无需输入"> |
| 可用的命令 | <例如 `python3 scratch/xxx.py --check`> |
| 不可用的东西 | <例如 不要联网 / 不要改库 / 不要读某些目录> |

## 三、必须做到

1. <>
2. <>
3. **只在 `answer.md` 里写结果**；不修改 `task.md`，不写记忆或其它任何文件。

## 四、验收标准（怎么判定对错）

- [ ] 明确写出用了什么命令 / 什么数据范围
- [ ] 关键数字给到<位数/单位>，并说明来源字段
- [ ] 结论与预期一致：<写出预期值或判定方法>
- [ ] 卡点或不确定之处如实说明（**不许猜**）
- [ ] 说清验到哪一层：逻辑层 / 文件层（sha256）/ 设备层（`dd iflag=direct`）/ 新鲜度层

## 五、交付物

- 只写 `answer.md`：结论、依据（命令与原始输出片段）、不确定的点与建议的下一步。
